# Requirements for Python 3.12 (Recommended - Best Compatibility)
# Using flexible versions to allow pip to resolve dependencies

# Core Framework
crewai>=0.28.8
langchain>=0.1.10
langchain-ollama>=0.1.0

# Dashboard
streamlit>=1.28.1
plotly>=5.18.0
pandas>=2.1.3
numpy>=1.26.2

# Telegram Bot
python-telegram-bot>=20.7

# Shoonya API
# Install from GitLab repository
git+https://gitlab.com/algo2t/shoonya.git

# Database
sqlalchemy>=2.0.23
alembic>=1.13.1
aiosqlite>=0.19.0

# Technical Analysis (29 indicators)
ta>=0.11.0
pandas-ta>=0.4.67b0

# Sentiment Analysis (Required)
beautifulsoup4>=4.12.2  # HTML parsing for article content extraction
feedparser>=6.0.10      # RSS feed parsing for news sources

# Sentiment Analysis (Optional - for FinBERT and VADER)
# Note: DeepSeek R1 7B (already in system) is the primary sentiment analyzer
# These are optional optimizations for faster processing of simple articles
textblob>=0.17.1        # Optional: Basic sentiment analysis (simple articles)
vaderSentiment>=3.3.2   # Optional: VADER sentiment analyzer (requires nltk)
nltk>=3.8.1             # Optional: Required for VADER sentiment
transformers>=4.35.0    # Optional: For FinBERT model (if using FinBERT for speed)
torch>=2.1.0            # Optional: Required for FinBERT (large download ~2GB)

# Data Sources
yfinance>=0.2.32
requests>=2.31.0

# WebSocket
websockets>=12.0

# Scheduling (for resilience cron jobs)
schedule>=1.2.0
APScheduler>=3.10.4

# Ollama Python Library
ollama>=0.1.0

# HTTP Client (for other services)
httpx>=0.25.2

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
loguru>=0.7.2
pyyaml>=6.0.1  # Required for orchestrator workflow and contract loading

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
pytest-asyncio>=0.21.0
